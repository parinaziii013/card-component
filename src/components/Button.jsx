const Button = ({ btn, btn2 }) => {
  return (
    <div>
      <button className="button">{btn}</button>
      <button className="button">{btn2}</button>
    </div>
  );
};
export default Button;
